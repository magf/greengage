x-common-service: &common-service
  image: "${IMAGE}"
  privileged: true
  sysctls:
    kernel.sem: 500 1024000 200 4096
  init: true
  ulimits:
    nofile: 65535
  working_dir: /home/gpadmin
  volumes:
    - ${PWD}/allure-results:/tmp/allure-results
    - ${PWD}/sqldump/dump.sql:/home/gpadmin/sqldump/dump.sql
    - ${PWD}/ssh_keys/id_rsa.pub:/home/gpadmin/.ssh/id_rsa.pub
    - ${PWD}/ssh_keys/id_rsa:/home/gpadmin/.ssh/id_rsa
    - ${PWD}/logs/$(hostname):/logs
  entrypoint: >
    sleep infinity

services:
  cdw:
    <<: *common-service
    hostname: cdw
  sdw1:
    <<: *common-service
    hostname: sdw1
  sdw2:
    <<: *common-service
    hostname: sdw2
  sdw3:
    <<: *common-service
    hostname: sdw3
